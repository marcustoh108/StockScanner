{
  "name": "stockscanner-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "StockScanner iOS/Android shell (Capacitor) around the StockScanner API.",
  "scripts": {
    "add:ios": "cap add ios",
    "add:android": "cap add android",
    "sync": "cap sync",
    "open:ios": "cap open ios",
    "open:android": "cap open android",
    "serve": "npx http-server www -p 8100"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "cordova-plugin-purchase": "^13.18.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  }
}
